# Arquivo de exemplo para variáveis de ambiente
# Copie este arquivo para .env e preencha com seus valores

# ===========================================
# CONFIGURAÇÕES N8N
# ===========================================

# Configurações básicas
N8N_HOST=0.0.0.0
N8N_PORT=5678
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/

# Timezone
GENERIC_TIMEZONE=America/Sao_Paulo

# Banco de dados
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgres
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_USER=n8n
DB_POSTGRESDB_PASSWORD=n8n

# Usuário padrão (usado apenas na primeira inicialização)
N8N_OWNER_EMAIL=admin@workshop.com
N8N_OWNER_PASSWORD=workshop123

# Segurança
N8N_USER_MANAGEMENT_JWT_SECRET=workshop-jwt-secret-change-in-production
N8N_SECURE_COOKIE=false

# ===========================================
# CONFIGURAÇÕES EVOLUTION API
# ===========================================

# Servidor
SERVER_TYPE=http
SERVER_PORT=8080
SERVER_URL=http://localhost:8080

# Autenticação
AUTHENTICATION_TYPE=apikey
AUTHENTICATION_API_KEY=workshop-evolution-key
AUTHENTICATION_GLOBAL_AUTH_TOKEN=global-workshop-key

# Banco de dados
DATABASE_CONNECTION_URI=postgresql://evolution:evolution@postgres-evolution:5432/evolution

# Redis
REDIS_URI=redis://redis-evolution:6379

# Webhook global
WEBHOOK_GLOBAL_URL=http://localhost:5678/webhook/evolution
WEBHOOK_GLOBAL_ENABLED=true

# ===========================================
# CONFIGURAÇÕES DO BANCO DE DADOS
# ===========================================

# PostgreSQL para N8N
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n

# PostgreSQL para Evolution API
POSTGRES_EVOLUTION_DB=evolution
POSTGRES_EVOLUTION_USER=evolution
POSTGRES_EVOLUTION_PASSWORD=evolution

# ===========================================
# CONFIGURAÇÕES DE PRODUÇÃO
# ===========================================

# Para produção, altere estas configurações:
# - Use senhas fortes e únicas
# - Configure HTTPS
# - Use URLs reais (não localhost)
# - Configure backup do banco
# - Use Redis com senha
# - Configure monitoramento
